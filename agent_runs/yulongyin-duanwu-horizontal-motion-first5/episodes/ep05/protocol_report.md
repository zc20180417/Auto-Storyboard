# Episode Validation

status: failed

## Horizontal Space Lock
- 第1组组首空间锁定包含过程动作 `从左侧洞口方向冲向 / 冲向`；横屏组首应只写第一帧静态结果状态，例如位于画面哪侧、身体朝向、视线关系和道具位置；请把走来、进入、爬行、逼近、拿起、放下等动作放到后续 `镜头描述`。
- 第2组组首空间锁定包含过程动作 `逼近 / 放下`；横屏组首应只写第一帧静态结果状态，例如位于画面哪侧、身体朝向、视线关系和道具位置；请把走来、进入、爬行、逼近、拿起、放下等动作放到后续 `镜头描述`。
- 第3组组首空间锁定包含过程动作 `逼近`；横屏组首应只写第一帧静态结果状态，例如位于画面哪侧、身体朝向、视线关系和道具位置；请把走来、进入、爬行、逼近、拿起、放下等动作放到后续 `镜头描述`。
