# Episode Validation

status: failed

## Horizontal Space Lock
- 第3组组首空间锁定包含过程动作 `从画面左侧前景拄拐挤入 / 挤入`；横屏组首应只写第一帧静态结果状态，例如位于画面哪侧、身体朝向、视线关系和道具位置；请把走来、进入、爬行、逼近、拿起、放下等动作放到后续 `镜头描述`。
- 第4组组首空间锁定包含过程动作 `从画面左后方低身挤入 / 挤入`；横屏组首应只写第一帧静态结果状态，例如位于画面哪侧、身体朝向、视线关系和道具位置；请把走来、进入、爬行、逼近、拿起、放下等动作放到后续 `镜头描述`。
