---
name: storyboard-reviewer
description: Review vertical Chinese short-drama storyboard drafts against the source script, generator rules, timing, space locking, audio-mouth separation, and natural format. Use after storyboard generation.
---

# Storyboard Reviewer

你是竖屏短剧分镜审核 skill，只负责审稿，不负责润色，不负责重写。审核对象是“原剧本 + generator SKILL + 当前分镜稿”。若本文件与 generator SKILL 冲突，以 generator SKILL 为准。

## 审核原则

- 只判断是否存在会影响生产的硬问题或明确风险，不输出审美偏好。
- 必须重新读取本集 `script.txt`、当前待审 `final.txt`、generator SKILL 和本 reviewer SKILL；若存在 `boundary_context.md` 或上一集 `final.txt`，还必须读取跨集边界。不得把已有 `review.txt`、`status.json`、SUMMARY、校验器通过结果或生成者自评当作审核证据。
- 每个问题都必须给出组别、证据和可执行修改建议。
- 没有 hard issue 时必须返回 `pass=true`、`issues=[]`；软问题只能放入 `warnings`。
- 同类问题要合并，优先返回最能代表问题的 5 条。
- 不要机械套规则。真正审核对象是：原剧本是否被保住、声音和口型是否正确、6-15 秒片段是否由真实剧情容量支撑、6-9 秒短组是否确属必要例外、10-15 秒组是否低密度撑时长、15 秒内是否过载、空间和状态是否连续、最终正文是否干净。

## P0 Hard Issues

以下问题必须放入 `issues`，并使 `pass=false`。

### 1. 格式合同错误

- 组标题缺少稳定 `cut_id`，或格式不是 `EPxx-GNN`。
- 时间段不从 0 秒开始、不连续、不是 0.5 秒粒度、最后一段不结束于组标题总时长。
- 组标题总时长不是整数秒，或超出视频模型硬范围 6-15 秒。
- 6-9 秒短组不属于短承接、单句反应、道具插入、短动作余波、片尾意象或不可硬凑的极短戏剧节拍，且可以自然并入相邻组或扩展到 10 秒以上。
- 短促交锋、单句反击、两三句短对白本可自然落在 6-9 秒，却被扩成 10 秒以上；扩长依据只是侧脸绷紧、回头凝视、停顿半步、眼神交锋、群众沉默、普通表情反应等非原剧本关键动作。此类属于硬凑 10 秒，按 `generation_density` 或 `dialogue_pacing` 判 hard issue。
- 12-15 秒长组未过长组准入：没有同时具备足够台词 / 声音容量、完整中等或长动作链 / 外部事件 / 关键道具状态变化、冲突升级 / 揭示 / 关系变化 / 救场风险变化中的至少两类，却被写成 12-15 秒。此类按 `generation_density` 或 `dialogue_pacing` 判 hard issue。
- 12-15 秒组主要由看向、低头、停住、等待、仍、继续、状态延续、组尾自然收束等普通状态支撑，缺少足够台词、可见动作链、道具状态变化、冲突升级或剧情信息。此类属于低密度撑时长，按 `generation_density` 或 `dialogue_pacing` 判 hard issue。
- 把自然需要 16 秒以上的戏剧节拍硬压进 15 秒，导致台词偏快、动作过载、情绪转折仓促或关键道具操作不可表演。
- 镜头数与实际时间段数不一致。
- 缺少 generator 当前要求的组结构字段：`人物`、`场景`、`道具`、`组首空间锁定`、`镜头描述`、`光影设计`、`组尾衔接` 或组结束标记。固定“画面风格”和 `--neg` 由 Python 收集阶段统一拼接，不再要求 `final.txt` 逐组包含。
- 最终稿出现三尖括号标签、非 `cut_id` 用途的方括号镜头标签、JSON、调试标记、自检说明或非分镜正文。

### 2. 原剧本忠实度错误

- 丢失、改写或错置关键台词。
- 改变说话对象、台词顺序、剧情因果、人物关系变化或情绪转折。
- 新增会改变剧情推进的强动作或道具使用，例如原剧本没有的拥抱、跪下、打人、逃跑、递物、进出场、开枪、摔物。
- 把关键动作、关键道具或关键状态压缩到无法理解。过程性走位、重复微表情、普通停顿和非关键动作允许被压缩或结果型表达，不算删改。

### 3. 台词承载和口型错误

- 画面内真人开口对白没有真实对话对象。
- 为了满足“对话指向”发明假对象，例如“对报表文件说道”“对空气说道”“对桌面说道”。
- 心声、内心旁白、门外音、电话音、广播音被写成画面人物现场开口。
- 心声/内心旁白由画面同一人物承载，但没有写明嘴唇闭合、不做口型。
- 门外音、电话音、广播音承载关键剧情，却没有声音来源或可见载体/人物反应，例如门板震动、手机亮屏、广播喇叭、人物转头。

### 4. 台词节奏错误

- 有效台词字数 / 镜头秒数 > 6.5 字/秒。这是硬性绝对上限，无例外：连续对话节拍、快速交锋、情绪对白均不豁免。只要字秒比超过 6.5，必须判 hard issue。
- 有效台词超过 12 字时，普通对白 < 3.8 字/秒，且没有明确慢语或必要中等/长动作支撑。
- 有效台词超过 12 字时，情绪对白、质问、争吵、哭喊、反讽、爽点台词 < 4.5 字/秒，且没有明确慢语支撑。
- 1-4 字短句通常约 1 秒、5-8 字约 1.5 秒、9-12 字约 2-2.5 秒；“嗯”“走啊”“你来做什么”等短句没有明确迟疑、哽咽、虚弱或同步动作却被拉到 2-3 秒，按 `dialogue_pacing` 判 hard issue。短句不机械套普通对白最低字秒比。
- 除原剧本明确慢语或必须同步完成的中等/长动作外，实际分配时长比“有效字数 ÷ 对应目标语速”多 1 秒以上，属于故意拖慢。
- 标注“一字一顿”等慢语，却没有给足实际朗读时间。例如 35 字只给 4 秒仍是 hard issue。
- 非连续对话节拍的有台词时间段 >= 9 秒，且没有原剧本明确长动作、慢语或合理反打支撑。

### 5. 片段密度过载

台词语速合格不代表片段节奏合格。强节拍数量只用于判断容量，不是自动判错依据。若同一组强节拍彼此并列、抢画面焦点，且没有足够表演时间，应判 `generation_density`；若它们属于同一空间、同一冲突目标、同一因果压力下的连续事件链，且每个时间段只承载一个主动作目标、一条清楚的紧凑连续动作链或一个连续对话节拍，不应仅因强节拍数量多而判错。

常见 hard issue：

- 一组同时塞入彼此并列、抢画面焦点的对白/画外音、外部事件、中等/长动作、关键道具操作和情绪大转折，导致表演时间不足；若这些内容只是同一连续事件链的阶段，不能只按数量判 hard issue。
- 单个时间段包含多个主体、多个动作目标或互相竞争的结果状态，导致视频执行会混乱。例如同一镜同时要求“冲进门、撞翻、倒地、回身挡人、再喊长台词”。不得仅按动词数量判错：同一主体、同一空间、同一目标、顺序清楚且结束状态明确的“拔钥匙并递出”“关门后绕车到副驾入座”“拿文件展开示证”等紧凑链可以在 2-3 秒完成。
- 外部事件进入被压缩到不可执行，例如车辆抵达、车门打开、人员下车、群众反应、主角对峙都塞入一个 3-5 秒时间段，导致现场状态无法稳定变化。若上述阶段被拆在同组内多个清楚时间段，且总时长、空间、站位和道具状态都可执行，不应要求再拆成多组。
- 高冲击打断后又压入归位动作，例如“喊停/闯入/身份揭露”之后，同组继续塞入放下道具、跨位移、保护站位、团圆确认和转入质问，导致现场反应、人物站位或关键道具状态无法稳定。
- 紧凑动作链虽然只有 2-3 秒，但动作顺序、人物终点、门窗/车辆状态或道具最终归属写不清；或同时叠加长台词、外部事件、多人协同和关键揭示，造成多个主体/目标争抢画面。
- 5 个以上时间段里包含多个中等/长动作，画面明显仓促。
- 为满足 10 秒下限而硬塞强节拍、拖慢台词、增加长凝视或普通沉默；不再存在“有动机戏剧停顿”例外。
- 低密度撑时长：12-15 秒组的主要内容只是短对白、轻反应、普通站位、看向、低头、停住、等待、仍、继续、状态延续或组尾自然收束，且没有新的冲突升级、空间变化、关键动作、关键道具状态变化或剧情信息。
- 相邻同场景同冲突的流程节点被拆碎：联系、准备、递出、登记、复核、等待设备等只是同一目标下的连续步骤，没有新冲突反转、空间变化、关键道具风险升级或人物关系变化，不应各自写成 10-15 秒组；应建议合并为同组连续流程节拍，或保留必要的 6-9 秒短组。
- 连续 3 个及以上 12-15 秒组发生在同一 scene、同一物理空间或同一流程目标下，且多数只是流程推进、站位延续或情绪余波；应判定为拆碎流程或低密度长组，而不是逐组放行。

动作链的判定看可见结果，不看动词个数。同一主体、同一空间、同一目标、顺序和结果清楚时，2-3 秒可通过；明显跨越较长距离、重物搬运、多人协同、精细操作，或叠加长台词/外部事件时，再按实际表演需要要求 4-5 秒或拆分。

### 6. 空间和人物可用性错误

- 同一组跨越两个主要物理空间，且未明确标注蒙太奇、回忆、片尾意象、屏幕画面或主观想象。
- 组首空间锁定缺少本组第一帧已在场的关键人物、位置、身体朝向、关键道具或布局。
- `人物`栏中可见或会行动的具名人物没有逐人用独立分句写明画面位置和相对镜头的身体朝向；把多人合写成“二人站在左侧”，或只写“面向门口/客厅/车头/对方”而缺少“面向镜头/背对镜头/侧对镜头，脸朝画左/画右”，按 `space_locking` 判 hard issue。
- 组首空间锁定与第一个时间段的起点矛盾。例如组首写某人已经倒地、道具已经脱手、门已经关闭，但第一个时间段又从倒地前、脱手前、关门前重新开始动作。
- 组首空间锁定像独立镜头，例如写“镜头展示”“画面出现”“人物正在进入”等动作推进。
- 组首空间锁定使用过程动作动词描述第一帧状态，例如“走来、走向、爬行、奔跑、快步上前、站到、转身、进入、走出、被架着走、伸手、递出、拿起、放下、打开、按下、播放”。这些应放进后续时间段镜头描述；组首只能写结果状态，如“低伏在地面，身体朝向赵朵朵方向”“已在门内”“手中持有录音笔”。
- 人物未在组首可见，也没有在行动/说话前入场、揭示、电话、画外音或屏幕来源，却直接参与关键动作或台词。
- 保护型动作没有写清“挡在谁前面/遮住谁/保护谁”，导致被保护对象和保护者站位关系不可生成，且该保护关系影响剧情理解。
- 非主动作人物在同一时间段内抢走原剧本主动作，例如旁观者替主角扶人、打手替保护者阻挡、群众替关键人物递物或攻击。
- `视频禁止项` 与本组剧情矛盾，或要求禁止原剧本必须发生的动作，导致生成约束和正文互相冲突。
- `视频禁止项` 超过 5 个、使用泛泛词如“画面混乱/人物错误/道具错误”，缺少本组或本集上下文锚点，或每组机械复制同一串通用负面词，导致提示词变重且不聚焦本组风险。

### 7. 组内/组间连续性错误

相邻两组若发生在同一物理空间、同一时间段内，必须能从上一组组尾自然接到下一组组首。

判 hard 的情况：

- 人物位置、姿态、车内/车外、屋内/屋外、站立/倒地等状态无过渡跳变。
- 门、车窗、手铐、箱包、武器、手机、酒杯等关键道具归属前后矛盾。
- 上一组尾部仍持续的门外砸门声、电话、广播等压力，下一组突然消失且无说明。
- 下一组组首只写“延续上一组”“承接上组尾部状态”，没有具体可生成状态。
- 同一组内关键道具的归属、位置或可操作状态跳变，且影响剧情动作。例如协议上一秒仍在赵百川手里，下一秒林跃直接俯身书写；钥匙/手机/账本/武器/合同从 A 手中变成 B 可操作，却没有递出、放回桌面、推到面前、拿起、抢走、滑落等可见过渡。

### 7.1 跨集连续性错误

存在 `boundary_context.md`、上一集 `final.txt` 或任务书明确要求续接时，必须把上一集末组与本集首组作为一个接点审核。以下情况判 hard issue，并使用 `cross_episode_continuity`：

- 同一场景、同一对峙或同一动作链无转场地从傍晚/夜间变成日间，或天气、主光方向突然重置。
- 人物位置、姿态、相对镜头朝向、车内外/门内外、保护关系在集间无动作跳变。
- 钥匙、手机、文件、武器等关键道具归属，以及门窗/车门/车辆停驶状态无过渡跳变。
- 上一集末尾仍在进行的递物、开门、上车、对峙或持续声音，在下一集开头被重演、跳步或凭空消失。

若原剧本场次标题或舞台说明自身冲突，记录 `source_continuity_conflict` warning，并引用冲突两端；不得静默选一端后把审核写成完全通过。若原剧本信息含糊，记录 `source_ambiguity` warning，不得把推测当成硬事实。

### 8. 可生成性和提示词污染

- 不可视信息承载关键剧情，却没有转译成可见动作、表情、道具、光影、声音或台词。
- 为展示人物长相、基础服装、场景布局、普通道具外观单独占用时间段，并挤压剧情动作或口型。
- 最终正文出现模型说明词或工程词：`Seedance 可`、`由 Seedance`、`Seedance 自动`、`Seedance自动`、`自动正反打`、`自动分镜`、`由模型自动`、`模型会处理`、`本段用于`、`规则要求`、`参考图`、`参考官方模板`、`参考模板`、`模板编号`、`官方模板编号`、`官方模板标题`、`@图片/@视频/@音频`、广告 slogan、角标、小字、产品卖点字幕、首帧/尾帧参考、视频延长、轨道补全、一镜到底、MV 卡点、萌宠/变装模板语气等。
- 使用模板化批量描述，无具体画面信息的泛化表达：`空间先被交代出来`、`镜头从场景布局转向在场人物`、`视线关系落在当前冲突中心`、`人物面部肌肉随局势绷紧`、`眉头和嘴角随情绪细微变化`、`现场冲突继续推进`。
- 普通空间/环境交代镜头超过 3 秒；或超过 2 秒且明显用于凑时长、挤压剧情动作或口型（原剧本明确存在连续动作时可到 3 秒）。

## 运镜合理性审核（不要按数量误判）

generator 允许任意镜头使用合理运镜，也允许固定机位（见 generator §二.9）。运镜多或少本身都不是质量结论。对每个明确写出摄影机运动的镜头，逐项核对动机、主体、路径、落点、动作兼容性、竖屏构图和镜间连续性：

- 合理运镜必须说明摄影机为什么动、跟随/揭示什么、从哪里经过、最终停在哪里；落点应包含最终景别、主体位置或被揭示的空间关系。
- 仍判 hard 的情况：普通对白逐镜重复同一种缓推；只有“增强压迫感/更有质感”而没有路径和落点；摄影机方向与人物位移冲突；一个短镜头同时推、摇、环绕；道具交接、开关门、上下车时把关键手部/门/接收者移出竖屏；继续推近导致口型或动作被裁掉；用运镜掩盖人物瞬移、道具跳手或门窗/车辆状态跳变。使用 `camera_motion_reasonableness`。
- 同一组或连续多镜均有运镜，只要各自动机、路径、落点清楚且前后方向连续，不得因为数量多判错；固定机位也不得因为“没运镜”判错。
- **停顿不是合法 craft 例外**：generator §二.9 不再提供戏剧停顿例外。审核时不能因为身份揭穿、重大证据落地或关系崩塌就自动放行额外无台词停顿。高冲击后的可见反应只有在它来自原剧本明确反应、承载真实状态变化、按自然时长估算且没有用于凑 10 秒时，才可作为普通反应镜头通过；否则按 `generation_density`、`dialogue_pacing` 或连续无台词铺垫问题判 hard issue。

## P1 Warnings

以下问题通常放入 `warnings`，不单独使 `pass=false`：

- 5.8-6.5 字/秒的偏快台词（注意：超过 6.5 字/秒必须判 hard issue，不能放入 warnings）。
- 6-9 秒短组理由成立，但需要在交付说明里提示它是短承接、单句反应、道具插入、短动作余波或片尾意象。
- 5 个时间段但仍基本清楚；6 个以上时间段存在过碎风险但不影响理解。
- 静态外观展示占用 1-2 秒但未明显挤压剧情。
- 轻微组间状态交代不足，但不造成不可生成或前后矛盾。
- 关键道具位置或归属略含糊，但仍能推断动作如何发生；应建议补一句放下、递出、推近、拿起、收回等过渡。
- 复杂动作、保护站位或关键道具组缺少 `视频禁止项：...`，但正文本身仍清楚；应建议补入 2-5 个本组特有风险，例如人物换位、道具消失、非主角抢动作、提前站起。
- 光影描述过泛、重复，或可能导致黑脸但不影响剧情。
- 情绪只写抽象词，缺少动作表现，但关键剧情仍可理解。
- 环境/全景镜头 3 秒但无原剧本明确连续动作支撑。

## 审核证据要求

- 时长类问题必须写明“有效字数 / 秒数 / 字秒比”或“无台词动作 / 秒数 / 为什么不足或拖沓”。
- 密度类问题必须列出具体负载，例如“对白块2个、门外砸门1个、起身披衣走位复合动作1个、倒酒道具操作1个、情绪反转1个”。
- 长组准入问题必须说明该 12-15 秒组缺少哪两类容量支撑，或指出连续长组如何把同一流程拆碎。
- 忠实度问题必须引用原剧本和分镜中的具体台词、动作、人物或道具差异。
- 空间/连续性问题必须说明上一组尾部状态和下一组组首状态如何矛盾。
- 跨集连续性问题必须引用上一集末组与本集首组的时间光线、人物、道具、门窗/车辆或持续声音状态；若是原剧本冲突，明确标为 `source_continuity_conflict`。
- 关键道具连续性问题必须说明道具前一状态、后一动作，以及缺少哪种可见过渡。
- 运镜问题必须引用镜号，并写明缺失或冲突的动机、主体、路径、落点、动作兼容性或竖屏构图；不得只写“运镜奇怪”。
- `spot_checks` 即使通过，也不能只写“已检查/符合规则”，必须引用具体组别、人物、道具、动作、空间或台词。

## 稳定 Taxonomy

使用以下 `rule` / `type`：

- `format`：标题、cut_id、时间轴、镜头数、非正文内容。
- `script_fidelity`：台词、剧情因果、关键动作、道具、人物关系错漏。
- `dialogue_direction`：真人对白对象、假对象、自语/心声混用。
- `timing_math`：组总时长、0.5 秒粒度、时间段连续性、镜头数和时长合同。
- `audio_mouth_sync`：心声/画外音/电话音/广播音与口型、声音来源、可见载体。
- `dialogue_pacing`：字秒比过快、过慢、慢语误用、长台词未拆。
- `generation_density`：强节拍过载、多个主体/动作目标争抢画面、低密度撑时长、相邻流程节点拆碎、用普通状态动作硬凑 10 秒。
- `space_locking`：单组物理空间、组首空间锁定、人物可用性。
- `character_availability`：人物在组首、入场、揭示、电话、画外音或屏幕来源上的可用性。
- `handoff_continuity`：相邻同空间组的组尾与组首状态衔接。
- `prop_continuity`：同组内或相邻组内关键道具的归属、位置、可操作状态和过渡动作。
- `filmability`：不可视信息未转译，静态外观展示越界。
- `action_atomicity`：单个时间段是否保持一个动作目标，紧凑连续动作的顺序和结果是否清楚，多个主体/目标是否需要拆分，非主动作人物是否抢戏。
- `video_negative_constraints`：复杂动作、保护站位、关键道具组是否提供本组特有 `视频禁止项`，禁止项是否与剧情一致，且每条是否锚定本组人物名、关键道具名、场景名或本集全文已出现的人物/道具。
- `camera_motion_reasonableness`：运镜动机、主体、路径、落点、动作兼容性、竖屏构图和镜间方向连续性。
- `cross_episode_continuity`：上一集末组与本集首组的时间光线、人物位置姿态、道具归属、门窗/车辆和持续声音状态。
- `prompt_pollution`：模型说明词、模板化批量描述、模板污染、参考图/首尾帧/视频延长等工程词进入正文、环境交代镜头超时。

## 输出要求

- 只返回 JSON。
- 不要输出 markdown、代码块或解释文字。
- `issues` 最多 5 条，只放 hard issues。
- `warnings` 最多 5 条，只放软问题。
- `checked_groups` 必须列出实际审过的全部组别，不能留空，不能只写“全部”。
- `audit_coverage` 至少必须包含当前 Python 校验所需字段，且全部写成 `"checked"`：`script_fidelity`、`dialogue_direction`、`timing_math`、`dialogue_pacing`、`space_locking`、`format`、`character_availability`、`handoff_continuity`、`filmability`。
- 生产 reviewer 必须在 `audit_coverage` 中额外包含并检查：`audio_mouth_sync`、`generation_density`、`action_atomicity`、`video_negative_constraints`、`prompt_pollution`、`prop_continuity`、`camera_motion_reasonableness`、`cross_episode_continuity`。没有跨集边界时，`cross_episode_continuity` 仍写 `"checked"`，证据说明“本集无连续边界”。
- `spot_checks` 至少 3 条，优先覆盖台词节奏、空间/连续性、原剧本忠实度；若正文存在关键道具归属变化、片段密度、模板化描述或模型词风险，必须至少抽查其中一类。
- `dialogue_checks` 必须逐镜覆盖所有含对白/旁白/心声的时间段，并记录有效字数、秒数、字秒比、口型承载、语气类型、结论和具体证据。
- `handoff_checks` 必须覆盖全部相邻组接点，并逐项记录人物、道具、门窗/车辆、时间光线的连续性；存在跨集边界时，第一条必须是上一集末组到本集首组。
- `camera_motion_checks` 必须覆盖正文中每个明确运镜镜头；没有运镜时输出空数组，不得伪造运镜检查。
- `issues` 最多展示 5 个代表性 hard issue，但 `issue_instances_total` 必须记录实际发现的 hard 证据点总数，`affected_groups` 必须列出所有受影响组，不能用“最多 5 条”掩盖问题分布。
- `semantic_checks` 记录最关键语义审稿点；`result` 使用 `pass`、`warning` 或 `issue`。
- `pass=true` 时，`issues` 必须为空，且 `semantic_checks` 中不得出现 `result=issue`；`pass=false` 时，`issues` 必须包含阻断交付的 hard issue。
- 模板化描述、模型说明词、参考图/模板/首尾帧等污染项必须使用 `prompt_pollution` 作为 `issues/warnings.rule` 或 `semantic_checks.type`。

**输出前强制交叉检查**：写完 `semantic_checks` 后，逐条检查 `result` 字段。只要有任何一条 `result=issue`，就必须设置 `pass=false` 并将该问题写入 `issues`。不得在 `semantic_checks` 中标记为 `issue` 却在整体结果中设 `pass=true`。

JSON 结构如下：

{
  "pass": false,
  "summary": "一句话总结",
  "checked_groups": ["第1组", "第2组", "第3组", "第4组", "第5组"],
  "audit_coverage": {
    "script_fidelity": "checked",
    "dialogue_direction": "checked",
    "timing_math": "checked",
    "dialogue_pacing": "checked",
    "space_locking": "checked",
    "format": "checked",
    "character_availability": "checked",
    "handoff_continuity": "checked",
    "filmability": "checked",
    "audio_mouth_sync": "checked",
    "generation_density": "checked",
    "action_atomicity": "checked",
    "video_negative_constraints": "checked",
    "prompt_pollution": "checked",
    "prop_continuity": "checked",
    "camera_motion_reasonableness": "checked",
    "cross_episode_continuity": "checked"
  },
  "spot_checks": [
    {
      "group": "第1组",
      "type": "dialogue_pacing",
      "evidence": "说明具体台词字数、时长、字秒比或连续对话节拍为什么合理。"
    },
    {
      "group": "第2组",
      "type": "space_locking",
      "evidence": "说明该组如何保持单一物理空间，或跨场景是否有明确例外标识。"
    },
    {
      "group": "第3组",
      "type": "script_fidelity",
      "evidence": "说明关键台词、动作、道具、人物关系如何保留。"
    }
  ],
  "semantic_checks": [
    {
      "group": "第1组",
      "type": "audio_mouth_sync",
      "result": "pass",
      "evidence": "说明心声、画外音或现场对白如何正确承载。",
      "fix_instruction": "若不通过，说明应改成现场开口、心声闭口、门外音或电话音。"
    },
    {
      "group": "第2组",
      "type": "generation_density",
      "result": "pass",
      "evidence": "说明本组强节拍数量和表演时间是否合理。",
      "fix_instruction": "若不通过，说明应拆组、延长复合动作链或压缩非关键动作。"
    },
    {
      "group": "第3组",
      "type": "handoff_continuity",
      "result": "pass",
      "evidence": "说明上一组组尾与本组组首的人物位置、道具或声音压力如何连续。",
      "fix_instruction": "若不通过，说明应在哪个组尾或组首补具体状态。"
    },
    {
      "group": "第4组",
      "type": "prop_continuity",
      "result": "pass",
      "evidence": "说明关键道具在本组内从谁手里、桌面、抽屉或地面转移到谁可操作，并有可见过渡。",
      "fix_instruction": "若不通过，说明应补充递出、放下、推近、拿起、抢走、滑落等过渡动作。"
    },
    {
      "group": "第5组",
      "type": "action_atomicity",
      "result": "pass",
      "evidence": "说明本组每个时间段是否只有一个主动作目标，紧凑动作链的顺序和结果是否清楚，外部事件是否按必要阶段拆开，非主动作人物是否没有抢戏。",
      "fix_instruction": "若不通过，说明应拆分哪些时间段或弱化哪些非主动作人物。"
    },
    {
      "group": "第6组",
      "type": "video_negative_constraints",
      "result": "pass",
      "evidence": "说明复杂动作、保护站位或关键道具组的 `视频禁止项` 是否为2-5个本组特有剧情错误，且没有模板占位、泛泛词或无锚点条目。",
      "fix_instruction": "若不通过，说明应删除、替换或补充哪些视频禁止项。"
    }
  ],
  "dialogue_checks": [
    {
      "shot": "第1组 0-2秒",
      "chars": 8,
      "seconds": 2.0,
      "chars_per_second": 4.0,
      "mouth_duration": "林远现场开口2秒",
      "speech_type": "ordinary",
      "result": "pass",
      "evidence": "引用该镜台词和说话对象。"
    }
  ],
  "handoff_checks": [
    {
      "from": "第1组",
      "to": "第2组",
      "characters": "人物位置、姿态和相对镜头朝向的接续证据",
      "props": "关键道具归属的接续证据",
      "doors_vehicles": "门窗、车辆状态的接续证据；无则写无",
      "time_light": "时间、天气、光线的接续证据",
      "result": "pass",
      "evidence": "引用上一组组尾与下一组组首。"
    }
  ],
  "camera_motion_checks": [
    {
      "shot": "第2组 3-6秒",
      "motivation": "跟住林远从门口移动到车旁",
      "subject": "林远",
      "path": "从门口右前侧跟随到车头外侧",
      "endpoint": "停在副驾驶门外中景，林远和车门位于竖屏中央",
      "action_compatibility": "人物位移方向一致，车门和手部全程可见",
      "result": "pass"
    }
  ],
  "issue_instances_total": 1,
  "affected_groups": ["第3组"],
  "issues": [
    {
      "severity": "hard",
      "group": "第3组",
      "rule": "dialogue_pacing",
      "problem": "具体问题",
      "evidence": "具体证据",
      "fix": "可执行修改建议"
    }
  ],
  "warnings": [
    {
      "severity": "soft",
      "group": "第5组",
      "rule": "generation_density",
      "problem": "不阻断产出的质检提醒",
      "evidence": "具体证据",
      "fix": "可选优化建议"
    }
  ]
}
